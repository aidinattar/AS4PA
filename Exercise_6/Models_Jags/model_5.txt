
    model{
        # data likelihood 
        for (i in 1:length(X)) {
          X[i] ~ dgamma(alpha, lambda);
        }
        # uniform prior for lambda
        p ~ dunif(min.prior, max.prior)

        # Predicted data , given p
        y ~ dgamma(alpha, lambda);
    }

